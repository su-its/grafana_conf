modules:
  if_mib:
    walk:
      - 1.3.6.1.2.1.2        # IF-MIB::ifTable
      - 1.3.6.1.2.1.31.1.1   # IF-MIB::ifXTable (HC counters)
    lookups:
      - source_indexes: [ifIndex]
        lookup: ifName
      - source_indexes: [ifIndex]
        lookup: ifAlias
    version: 2
    auth:
      community: public
